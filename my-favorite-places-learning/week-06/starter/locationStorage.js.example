// Week 6 Starter: LocalStorage Utilities
// Create this file in src/utils/locationStorage.js

// Get all locations from localStorage
export function getLocations() {
  // TODO: Get 'myFavoritePlaces' from localStorage
  // Parse the JSON and return it, or return empty array if nothing exists
}

// Save a new location
export function saveLocation(location) {
  // TODO: Get existing locations
  // Add the new location with an ID
  // Save back to localStorage
  // Return the updated array
}

// Delete a location by ID
export function deleteLocation(id) {
  // TODO: Get existing locations
  // Filter out the location with matching id
  // Save back to localStorage
  // Return the updated array
}

// Update an existing location
export function updateLocation(id, updatedData) {
  // TODO: Get existing locations
  // Find and update the location with matching id
  // Save back to localStorage
  // Return the updated array
}

